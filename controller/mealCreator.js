import index from '../index.js'


export const mealcreator = async (body, id, res) => {
    console.log("Back-end modataget:", body, "&", id);

    // const createMeal = await index.connectedDatabase.postIntoDbMeal()
    // const addIngredients = await index.connectedDatabase.postIntoDbMealIngredient()
}